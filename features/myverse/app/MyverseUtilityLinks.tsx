"use client";

// 상단 탭 메뉴에 템플릿·AI 브리핑이 포함된 이후 이 컴포넌트는 빈 래퍼로 유지
// (하위 호환성 — IndexView 등에서 import하고 있어 삭제 대신 빈 렌더)
export function MyverseUtilityLinks({ className: _ = "" }: { className?: string }) {
    return null;
}
